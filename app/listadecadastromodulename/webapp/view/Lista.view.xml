<mvc:View controllerName="listadecadastromodulename.controller.Lista"
    xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	height="100%"   
	xmlns:f="sap.ui.layout.form" 
    xmlns="sap.m">
	<Page
		title="Page"
		class="sapUiContentPadding" >
		<customHeader>
			<Toolbar>
				<Button type="Back" press="onPress" />
				<ToolbarSpacer/>
				<Title text="Title" level="H2"/>
				<ToolbarSpacer/>
				<Button icon="sap-icon://edit" type="Transparent" press="onPress" ariaLabelledBy="editButtonLabel"/>
			</Toolbar>

		</customHeader>
		<subHeader>
			<Toolbar>
				<ToolbarSpacer/>
				<Button text="Default" press="onPress" />
				<Button type="Reject" text="Reject" press="onPress" />
				<Button icon="sap-icon://action" type="Transparent" press="onPress" ariaLabelledBy="actionButtonLabel"/>
				<ToolbarSpacer/>
			</Toolbar>

		</subHeader>
		<content>
			<HBox>
				<Button text="Default"
						press="onPress"
						ariaDescribedBy="defaultButtonDescription genericButtonDescription">
					<layoutData>
						<FlexItemData growFactor="1" />
					</layoutData>
				</Button>
				<Button type="Accept"
						text="Accept"
						press="onPress"
						ariaDescribedBy="acceptButtonDescription genericButtonDescription">
					<layoutData>
						<FlexItemData growFactor="1" />
					</layoutData>
				</Button>
				<Button type="Reject"
						text="Reject"
						press="onPress"
						ariaDescribedBy="rejectButtonDescription genericButtonDescription">
					<layoutData>
						<FlexItemData growFactor="1" />
					</layoutData>
				</Button>
				<Button text="Coming Soon"
						press="onPress"
						ariaDescribedBy="comingSoonButtonDescription genericButtonDescription"
						enabled="false">
					<layoutData>
						<FlexItemData growFactor="1" />
					</layoutData>
				</Button>

			</HBox>

			<!-- Collection of labels (some of which are invisible) used to provide ARIA descriptions for the buttons -->
			<Label id="genericButtonDescription" text="Note: The buttons in this sample display MessageToast when pressed."/>

			<core:InvisibleText id="defaultButtonDescription" text="Description of default button goes here."/>
			<core:InvisibleText id="acceptButtonDescription" text="Description of accept button goes here."/>
			<core:InvisibleText id="rejectButtonDescription" text="Description of reject button goes here."/>
			<core:InvisibleText id="comingSoonButtonDescription" text="This feature is not active just now."/>
			<!-- These labels exist only to provide targets for the ARIA label on the Edit and Action buttons -->
			<core:InvisibleText id="editButtonLabel" text="Edit Button Label"/>
			<core:InvisibleText id="actionButtonLabel" text="Action Button Label"/>
		</content>
		<footer>
			<Toolbar>
				<ToolbarSpacer/>
				<Button type="Emphasized" text="Emphasized" press="onPress" />
				<Button text="Default" press="onPress" />
				<Button icon="sap-icon://action" type="Transparent" press="onPress" />
			</Toolbar>
		</footer>
	<Input  id="Label" value="{oModelLista>/variavelInput}" liveChange="onDigitando"></Input>

    <List 
        headerText="Products"
        headerLevel="H2"
        items="{oModelLista>/Lista}" >
            <StandardListItem 
                title="{oModelLista>ID} - {oModelLista>name}"
                counter="">
            </StandardListItem>
    
    </List>

    <List 
        headerText="Products"
        headerLevel="H2"
        items="{
			path : '/Cadastro'
		}" >
            <StandardListItem 
                title="{ID}"
                counter="">
            </StandardListItem>
    
    </List>

    <List 
        headerText="Products"
        headerLevel="H2"
        items="{oModelLista>/Lista2}" >
            <StandardListItem 
                title="{oModelLista>ID} - {oModelLista>name}"
                counter="">
            </StandardListItem>
    
    </List>		

	<Input id="inputID" placeholder="Digite o ID" />

	<Button text="Buscar" press="onBuscar" />	

	<List items="{resultado>/Lista}">
		<StandardListItem title="{resultado>ID} - {resultado>nome}" />
	</List>	

	<Button text="Incluir Cadastro" press="onOpenDialog" />

	<Dialog id="dialogCadastro" title="Novo Cadastro">

		<VBox class="sapUiSmallMargin">

			<Input id="inputIDDialog" placeholder="ID" type="Number" />

			<Input id="inputNomeDialog" placeholder="Nome" />

			<Input id="inputCpfDialog" placeholder="CPF" />

		</VBox>

		<beginButton>
			<Button text="Cadastrar" press="onCriarCadastroDialog" type="Emphasized"/>
		</beginButton>

		<endButton>
			<Button text="Cancelar" press="onCloseDialog" />
		</endButton>

	</Dialog>

	<Input id="inputCep" placeholder="Digite o CEP" />

	<VBox class="sapUiSmallMarginTop">
		<Button text="Buscar" press="onBuscarCEP" />

		<VBox class="sapUiSmallMarginTop">
			<Label text="Logradouro" />
			<Text text="{cep>/logradouro}" />

			<Label text="Bairro" />
			<Text text="{cep>/bairro}" />

			<Label text="Cidade" />
			<Text text="{cep>/localidade}" />

			<Label text="UF" />
			<Text text="{cep>/uf}" />
		</VBox>
	</VBox>

	<f:SimpleForm
		editable="false"
		layout="ResponsiveGridLayout"
		class="sapUiSmallMarginTop">

		<f:content>
			<Button text="Buscar" press="onBuscarCEP" />

			<Label text="Logradouro" />
			<Text text="{cep>/logradouro}" />

			<Label text="Bairro" />
			<Text text="{cep>/bairro}" />

			<Label text="Cidade" />
			<Text text="{cep>/localidade}" />

			<Label text="UF" />
			<Text text="{cep>/uf}" />
		</f:content>
	</f:SimpleForm>	

	</Page>
</mvc:View>